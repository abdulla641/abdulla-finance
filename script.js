let data=JSON.parse(localStorage.getItem('finance'))||[];let credit=JSON.parse(localStorage.getItem('credit'))||[];let savings=Number(localStorage.getItem('savings'))||0;let currency=localStorage.getItem('currency')||'QAR';let chart,reportChart;
function val(id){return document.getElementById(id).value}function money(v){return currency+' '+Number(v).toLocaleString()}function setCurrency(){currency=val('currency');localStorage.setItem('currency',currency);render()}function save(){localStorage.setItem('finance',JSON.stringify(data))}
function addTransaction(){let name=val('name'),amount=Number(val('amount')),type=val('type'),category=val('category');if(!name||!amount)return;data.push({id:Date.now(),name,amount,type,category,date:new Date().toISOString()});save();render()}
function formatDate(d){return new Date(d).toLocaleString('en-GB',{day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit'})}
function addSaving(){let a=Number(val('saving'));if(a){savings+=a;localStorage.setItem('savings',savings);render()}}
function addCredit(){let person=val('person'),amount=Number(val('creditAmount')),type=val('creditType');if(!person||!amount)return;credit.push({id:Date.now(),person,amount,type,settled:0,date:new Date().toISOString()});localStorage.setItem('credit',JSON.stringify(credit));render()}
function settleCredit(id){let x=credit.find(i=>i.id===id),a=Number(prompt('Settlement amount'));if(x&&a){x.settled=Math.min(x.amount,x.settled+a);localStorage.setItem('credit',JSON.stringify(credit));render()}}
function editItem(id){let x=data.find(i=>i.id===id);if(x){x.name=prompt('Edit description',x.name)||x.name;x.amount=Number(prompt('Edit amount',x.amount))||x.amount;save();render()}}
function removeItem(id){data=data.filter(x=>x.id!==id);save();render()}
function exportData(){let b=new Blob([JSON.stringify({data,credit,savings,currency},null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='abdulla-finance-backup.json';a.click()}
function drawCharts(i,e){let r=document.getElementById('reportChart');if(r&&typeof Chart!=='undefined'){if(reportChart)reportChart.destroy();reportChart=new Chart(r,{type:'bar',data:{labels:['Income','Expense','Savings'],datasets:[{data:[i,e,savings]}]}})}}
function render(){let i=0,e=0;data.forEach(x=>{x.type==='income'?i+=x.amount:e+=x.amount});let income=document.getElementById('income'),expense=document.getElementById('expense'),balance=document.getElementById('balance'),net=document.getElementById('netWorth');if(income)income.textContent=money(i);if(expense)expense.textContent=money(e);if(balance)balance.textContent=money(i-e);if(net)net.textContent=money(i-e+savings);let list=document.getElementById('list');if(list)list.innerHTML=data.map(x=>`<li>${x.name} ${money(x.amount)}<br><small>${x.type==='income'?'🟢':'🔴'} ${x.category} | 📅 ${formatDate(x.date||Date.now())}</small><br><button onclick="editItem(${x.id})">Edit</button><button onclick="removeItem(${x.id})">Delete</button></li>`).join('');let cl=document.getElementById('creditList');if(cl)cl.innerHTML=credit.map(x=>`<li>${x.person} ${x.type==='receive'?'🟢':'🔴'} ${money(x.amount-x.settled)}<br><small>📅 ${formatDate(x.date||Date.now())}</small><button onclick="settleCredit(${x.id})">Settle</button></li>`).join('');drawCharts(i,e)}render();