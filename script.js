let data=JSON.parse(localStorage.getItem('finance'))||[];
function addTransaction(){let name=document.getElementById('name').value;let amount=Number(document.getElementById('amount').value);let type=document.getElementById('type').value;if(!name||!amount)return;data.push({name,amount,type});localStorage.setItem('finance',JSON.stringify(data));render();}
function render(){let income=0,expense=0;let list=document.getElementById('list');list.innerHTML='';data.forEach(x=>{if(x.type==='income')income+=x.amount;else expense+=x.amount;let li=document.createElement('li');li.textContent=`${x.name}: ${x.type==='income'?'+':'-'} QAR ${x.amount}`;list.appendChild(li)});document.getElementById('income').textContent='QAR '+income;document.getElementById('expense').textContent='QAR '+expense;document.getElementById('balance').textContent='QAR '+(income-expense)}render();